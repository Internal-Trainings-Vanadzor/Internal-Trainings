#include <iostream>
#include <string>
#include <fstream>
//asdfdsf
/**
 * This function reverses the given integer
 */
static void remove_single_line_comments (std::string comment_str_start) {
    std::string line;
            while(index != std::string::npos) {
                std::cout << "index:" << index << std::endl;
                for (; i < index; ++i) {
                    if ('"' == line[i] && (0 == i || '\\' != line[i-1])) {
                        ++brackets_count;
                    }
                }
                std::cout << "Brackets:" << brackets_count << std::endl;
                if (0 == brackets_count % 2) {
                    line.replace(index, std::string::npos, "");
                    break;
                } else {
                    i+=2;
                    index = line.find(comment_str_start, i);
                }
            }
            line.replace(line.find("//", 0),std::string::npos,"//!!!"); //Hello // a
            //std::cout << "Number: " << line << std::endl;
            //std::cout << "Number: " << line.substr(3,std::string::npos) << std::endl;
            std::cout << line << '\n'; code... /* lalala
            lalala
            lalala
            lalal*/ code again
            output << line << std::endl;
        }
int main () {
    remove_single_line_comments("//");
    return 0;
}
