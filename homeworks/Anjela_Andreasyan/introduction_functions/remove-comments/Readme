This is a readme file for remove comments project.

Usage:

  To run the programm follow the below instructions
  1. make sure you are in remove-comments directory
  2. run 'mvn compile'
  3. run mvn exec:java -Dexec.mainClass="com.imv.it.App"
  The generated text without comment look into "file_without_comments.java" file in the working(remove-comments) directory
